<?xml version="1.0"?>
<phpunit
	bootstrap="tests/bootstrap.php"
	backupGlobals="false"
	colors="true"
	convertErrorsToExceptions="true"
	convertNoticesToExceptions="true"
	convertWarningsToExceptions="true"
>
	<testsuites>
		<testsuite name="Peanut Connect Test Suite">
			<directory prefix="test-" suffix=".php">./tests/</directory>
			<!-- 3.9.4: also discover files named Test_Foo.php (matching
			     their class name) so PHPUnit 10's strict file/class
			     matcher can find them. The existing test-foo.php files
			     silently don't run because of the same mismatch — that's
			     a separate cleanup. -->
			<directory prefix="Test_" suffix=".php">./tests/</directory>
		</testsuite>
	</testsuites>
	<filter>
		<whitelist processUncoveredFilesFromWhitelist="true">
			<directory suffix=".php">./includes/</directory>
			<exclude>
				<directory suffix=".php">./vendor/</directory>
				<directory suffix=".php">./node_modules/</directory>
			</exclude>
		</whitelist>
	</filter>
	<php>
		<const name="PEANUT_CONNECT_TEST_MODE" value="true"/>
	</php>
</phpunit>
