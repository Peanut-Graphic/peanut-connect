# Peanut Ecosystem — Peanut Connect test + security gate.
# Derived from DOCUMENTATION/testing-standard/ci-templates/php-plugin-tests.yml
# and vitest-tests.yml. PHP unit+property tests run without a WordPress boot
# (Brain-Monkey-style mocks in tests/mocks/). Frontend runs vitest.
name: tests

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  static-and-security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: "8.3"
          tools: composer
      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist
      - name: Validate Composer audit transport contract
        run: |
          bash scripts/tests/composer-audit-transport.test.sh
          bash scripts/tests/composer-npm-audit-workflow.test.sh
      - name: Verify declared PHP support
        run: php scripts/verify-php-runtime.php
      - name: Dependency audit (PHP)
        run: bash scripts/run-dependency-audit-transport.sh composer

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Install locked Python auditor
        run: |
          python -m venv "$RUNNER_TEMP/dependency-audit-venv"
          "$RUNNER_TEMP/dependency-audit-venv/bin/python" -m pip install \
            --require-hashes \
            --no-deps \
            -r requirements-dependency-audit.lock

      - name: Validate dependency-audit transport contract
        run: |
          bash scripts/tests/dependency-audit-transport.test.sh
          bash scripts/tests/dependency-audit-workflow.test.sh
          bash scripts/tests/wp-contract-image-pins.test.sh

      - name: Audit the locked Python auditor
        run: |
          bash "$GITHUB_WORKSPACE/scripts/run-pip-audit.sh" "$RUNNER_TEMP/dependency-audit-venv/bin/pip-audit" -r requirements-dependency-audit.lock --no-deps --disable-pip

      - name: Audit the locked platform validator
        run: |
          bash "$GITHUB_WORKSPACE/scripts/run-pip-audit.sh" "$RUNNER_TEMP/dependency-audit-venv/bin/pip-audit" -r .peanut/platform/requirements.lock --no-deps --disable-pip
      - name: Install checksum-locked gitleaks
        uses: ./.github/actions/install-gitleaks

      - name: Secret scan (gitleaks)
        run: gitleaks detect --no-git --source . --redact --no-banner --exit-code 1

  php-tests:
    runs-on: ubuntu-latest
    needs: static-and-security
    env:
      # Main measured 14.26% statement coverage on 2026-08-29 after the legacy
      # API contract rewrite. Keep roughly five points of headroom for honest
      # source/test movement while preventing a return to unmeasured coverage.
      MIN_COVERAGE: "9"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup PHP (with pcov for coverage)
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: "8.2"
          coverage: pcov
          tools: composer

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Root Unit + Property tests + coverage
        run: vendor/bin/phpunit --testsuite=Unit,Property --coverage-clover=coverage/clover.xml

      - name: Legacy module unit tests
        run: vendor/bin/phpunit -c phpunit.legacy-unit.xml

      - name: Coverage gate
        run: php coverage-gate.php coverage/clover.xml "$MIN_COVERAGE"

      - name: Property suite (Net 6, explicit)
        run: vendor/bin/phpunit --testsuite=Property

      - name: Contract suite (Net 7, non-blocking until WP harness wired)
        continue-on-error: true
        run: vendor/bin/phpunit --testsuite=Contract

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: php-coverage
          path: coverage/

  php-minimum-tests:
    runs-on: ubuntu-latest
    needs: static-and-security
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup declared minimum PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: "8.0"
          coverage: none
          tools: composer

      - name: Install dependencies from the lockfile
        run: composer install --no-interaction --prefer-dist

      - name: Verify declared PHP support
        run: php scripts/verify-php-runtime.php --expect-runtime=8.0

      - name: Unit + Property tests at the declared minimum
        run: vendor/bin/phpunit --testsuite=Unit,Property

  frontend-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22.22.2"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Verify Node/npm contract
        run: test "$(npm --version)" = "10.9.7" && npm run runtime:verify
      - name: Validate npm audit transport contract
        run: bash ../scripts/tests/npm-audit-transport.test.sh
      - name: Install dependencies
        # legacy-peer-deps: eslint 10 vs eslint-plugin-jsx-a11y peer range
        # (pre-existing; tracked in known-gaps).
        run: npm ci --legacy-peer-deps
      # Net 2: blocking — ratcheted 2026-08-18, level corrected 2026-08-20.
      # Moderate, not high: npm grades GHSA-55q2-fjhq-7xh7 (the dompurify XSS
      # this branch exists to close) "moderate", and at --audit-level=high the
      # gate exits 0 on the vulnerable pin — verified by execution. Tree is
      # clean at moderate today.
      - name: Dependency audit (frontend)
        run: bash ../scripts/run-dependency-audit-transport.sh npm
      - name: Vitest (incl. Net 6 property tests)
        run: npx vitest run

  release-readiness:
    name: Release readiness
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      # nothing published. It proves the artifact the publisher WOULD build is
      # coherent -- version constants that agree with the header, every INCLUDE
      # path present, vendor/ shipped where the build needs it, a changelog
      # entry for this version, and a stable-tag line the publisher's bump can
      # actually match.
      #
      # The checker is synced here from peanut-meta by configs-sync, so this job
      # needs no credential to reach that private repo. Drift is reported by
      # configs-sync and surfaced in the weekly audit. PAR-406.
      - name: Could this be released at all?
        run: bash .peanut/release/verify-release-readiness.sh peanut-connect
