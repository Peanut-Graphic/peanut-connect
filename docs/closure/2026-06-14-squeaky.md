# Squeaky closure sweep — PEANUT-CONNECT — 2026-06-14

```
Repo: PEANUT-CONNECT (Peanut End to End · slug peanut-connect · WP plugin)
Released version: 3.14.0   ·   origin/main @ 90f6415 (v3.14.0 bump)   ·   update API serving 3.14.0 ✓ (download URL HTTP 200)

Local main == origin/main:   ✓
Production == origin/main:    ✓  v3.14.0 cut + published + license-server option bumped + verified live via update API
Checks (origin/main):        tests ✓ (112)  ·  composer audit --locked ✓ clean  ·  CI ✓ green  ·  lockfile ✓ clean  ·  fatal-refs ✓ clean

Open PRs: none     Open issues: 2 (#55, #56 — both intentional deferrals, not blockers)
Red CI runs: none     Stash: none     Unmerged branches: none
Needs a decision: none — release shipped
```

## Verified healthy
- **Tests:** full PHPUnit suite **112 passing**, 1 pre-existing skip. CI green on `origin/main`.
- **Dependencies:** `composer audit --locked` → no advisories.
- **Supply chain:** no poisoned lockfile entry.
- **Ship gate:** fatal-references sweep clean.
- **Tree/branches/stashes:** clean — nothing in flight, nothing undecided.

## What landed since the last sweep (the v3.13.0 → now arc)
The CAT backlog (D-10–D-13) was worked through end-to-end:
- **D-10** protocol version header (`X-Peanut-Protocol`) — edge #57 + hub #501 — issue #53 closed.
- **D-11** outbound edge→Hub signing (additive) — edge #58 + hub #502 — issue #54 closed.
- **D-12** key rotation + revocation — edge #60 + hub #503 (Phase 1) + hub #504 (integration test). Code-complete both repos; #55 open only for Phase 3 enablement.
- **D-13** deferred (#56).

## ✅ Blocker resolved — v3.14.0 shipped

The single blocker from the first pass (edge D-10/D-11/D-12 merged but unreleased) is **closed**:
- Bumped **3.13.0 → 3.14.0** (MINOR — new edge features, all additive/backward-compatible).
- Packaged via `scripts/package.sh` from a clean `origin/main` worktree (guardrails passed: version-match, fatal-refs sweep, no-dirty-tree).
- `gh release create v3.14.0` — published.
- License-server option bumped: `wp option update peanut_peanut-connect_version 3.14.0` (after Tailscale re-auth).
- **Verified live:** the update API serves `3.14.0`, `update_available:true`, download URL returns HTTP 200.
- **Safe regardless of Hub deploy state:** D-10 tolerates a missing header (v1); D-11 still sends Bearer (Hub accepts it); D-12 rotation is inert until the Hub signals `rotate` or an admin clicks Rotate (and a premature trigger fails gracefully to "staying on current key"). Shipping edge 3.14.0 cannot break a site even with the Hub side undeployed.

## Cross-repo note (out of this repo's scope, but relevant)
The Hub (peanut-hub) D-10/D-11/D-12 changes are merged to **its** main but their **prod deploy** is a separate repo's concern. D-12 only *functions* end-to-end once the Hub is deployed; until then the edge 3.14.0 features are inert/additive (see above). The Hub deploy + D-12 Phase 3 enablement (default rotation interval, staged like A8b) are the remaining operational steps, tracked by #55.

## STATUS: ✅ Clean

Tree clean · main == origin/main · production (latest release + update API) == origin/main, serving v3.14.0 · all checks green · no open blocker PRs/issues. The full microscope→remediation arc (v3.12.0 audit P0s → v3.13.0 at-rest encryption → v3.14.0 D-10/D-11/D-12) is shipped and live. The only open items — D-12 Phase 3 enablement (#55) and D-13 (#56) — are deliberate, tracked deferrals.

---

## §7 — Advisory pass (CAT + MAX on the finished report)

Two strategic agents read this report. Their voices kept distinct below.

### Correction first: MAX's one false alarm
MAX flagged that the Hub's `/sites/rotate` rotation routes "don't exist," concluding **D-12 is a dead button**. **This is wrong — it was a stale-checkout misread.** MAX grepped the shared checkout `~/Documents/Peanut/HUB`, which currently sits on branch `chore/bloxy-ui-adoption` (0 rotation routes). Verified at the **source of truth** (`peanut-hub` origin/main): `routes/api.php` carries `POST /sites/rotate` → `SiteRotationController::propose` and `/sites/rotate/confirm` → `confirm`; `SiteRotationController.php` and `KeyRotationHandshakeTest.php` are present; the signer emits `X-Peanut-Protocol`. The passing Hub integration test (#504) already proves these routes exist. **D-12 is complete on Hub main.** Lesson re-confirmed: verify at origin/main, never the ambient checkout.

### MAX — was the right thing done, and is it actually closed? (valid points)
- **The repo itself is genuinely closed.** Release is live, additive, backward-compatible, can't break a site. Ship/no-ship read: shipped, correct.
- **The real remaining action is cross-repo, not in this repo.** D-12 only *functions end-to-end* once **peanut-hub is prod-deployed**. Until then the edge features are inert/additive by design. Tracked by **#55**.
- **Hygiene:** the stale local checkout `~/Documents/Peanut/PEANUT-CONNECT` still shows 3.13.0 — pull it to 3.14.0 so the next session doesn't misread state.

### CAT — what could be next? (highest-leverage follow-ons)
- **Shared test vectors are one-directional.** `hub-signing-vectors.json` only covers Hub→edge. With D-11 (edge→Hub) and D-12 (rotation handshake) now shipped, extend the "shared referee" to cover **all three message types** — cheap now, expensive once the repos drift. *(1–2 days, do soon.)*
- **The pre-pairing window was never audited.** The microscope started *post-pairing*. The security-sensitive moment is **activation → successful pairing** (Hub-aware but not paired). Most likely place for a live leak — worth a dedicated persona pass.
- **Itron / `PEANUT_HUB_BLIND` build is the next frontier, and it's underspecced.** D-10's protocol header is the natural seam for a Hub-blind build. Don't approach Itron with a "handle updates manually" plan — script a Hub-blind release variant (Vite flag + separate Actions job). **Open question:** is Itron already live on a plugin version? If so this is a blocker, not a deferral.
- **Operational quick-win:** the HMAC canonical string includes a timestamp/replay window but the **clock-skew tolerance isn't documented** — cryptic auth failures on NTP-drifted shared WP hosts. Document the window + emit a skew diagnostic on auth failure. *(~hours, prevents a class of support tickets.)*
- **Update-channel blast radius (keep-in-mind):** the plugin update manifest comes from `peanutgraphic.com`. If that shares a domain/key blast radius with the Hub, a Hub compromise = update-manifest compromise. Keep the update-signing key separate from the Hub↔Edge auth key.

### Synthesis — What was done / What's next
**Done:** First-ever microscope + full remediation across three releases — request signing + close-default perms + nonce TOCTOU (v3.12.0), at-rest key encryption (v3.13.0), protocol-version negotiation + outbound signing + confirmed key rotation/revocation with a cross-repo integration test (v3.14.0). Edge is live; Hub counterparts merged to its main.

**What's next (in priority):**
1. **Deploy peanut-hub to prod** so D-12 functions end-to-end (#55) — the one real action gating the rotation feature.
2. Pull the stale local PEANUT-CONNECT checkout to 3.14.0 (hygiene).
3. Extend `hub-signing-vectors.json` to edge→Hub + rotation handshake (1–2 days).
4. Audit the pre-pairing activation window (new persona pass).
5. Resolve the Itron `PEANUT_HUB_BLIND` question — confirm whether Itron is live first.
6. Document HMAC clock-skew tolerance + add a skew diagnostic.

Items 3–6 are net-new follow-ons, not closure blockers. This sweep's repo is **Clean**.
